Git
- Distributed Version Control System (DVCS)
- Tracks source code changes
- Supports collaboration
- Works offline

Git      -> Version Control Tool
GitHub   -> Remote Repository Hosting

Distributed Version Control System vs Centralised Version Control System

CVCS
- Central server
- Internet required
Ex: SVN, Perforce

DVCS
- Full repository on every machine
- Offline work possible
Ex: Git

-------------------------------------------------------------------------------
.gitignore

Used to ignore files/folders that Git should not track.

Examples:
node_modules/
.env
build/
dist/

-------------------------------------------------------------------------------
Tracked vs Untracked

Tracked   -> Git monitors the file
Untracked -> Exists locally but Git doesn't track it

===============================================================================
Commit Internals - What happens during git commit?
===============================================================================

                                     Staging Area
                                          |
                                          v
                                    Create Blob(s) (.git)
                                          |
                                          v
                                    Create Commit Object
                              (author, message, timestamp, parent)
                                          |
                                          v
                                    Generate Commit Hash (SHA)
                                          |
                                          v
                                    Branch & HEAD move to new commit

===============================================================================
Branching & HEAD pointer
===============================================================================

Branch

- Independent line of development.
- Branch = Basically a Named pointer to latest commit, not a collection of commits

A --- B --- C
            ^
          main

main -> C

Git reconstructs history by following parent pointers.
main points to C -> C points to B -> B points to A

-------------------------------------------------------------------------------
HEAD

HEAD points to the currently checked-out branch.

HEAD -> main -> latest_commit

===============================================================================
Detached HEAD
===============================================================================

Occurs when checking out a commit instead of a branch.

git checkout <commit>

A --- B --- C
            ^
          HEAD

Normal:
HEAD -> main -> C

Detached:
HEAD -> C

-------------------------------------------------------------------------------
Commit in Detached HEAD

A --- B --- C --- D
            ^     ^
         main    HEAD

- HEAD moves to D
- No branch points to D
- D may become unreachable

-------------------------------------------------------------------------------
How to Return to Branch again -->  git switch main

A --- B --- C
            ^
          main
            ^
          HEAD

-------------------------------------------------------------------------------
Keep Detached HEAD Commit

A --- B --- C
      \
        D
        ^
      HEAD

git switch -c new-branch

===============================================================================
Undo Changes -- git reset vs git revert
===============================================================================

git reset
- Moves branch pointer backward
- Rewrites history
- Use for the Local commits only

A --- B --- C --- D

git reset --hard B

A --- B

--------------------------------------------------

git revert

- Creates a new commit that reverses the previous changes
- Safe for shared branches

A --- B --- C --- D --- E

E = Reverse of D

===============================================================================
Undo Last Commit
===============================================================================

git reset --soft HEAD~1   --> VS Code's Undo last commit
- Removes the commit
- But Keep changes STAGED

git reset HEAD~1
- Removes the commit
- Keep changes UNSTAGED

git reset --hard HEAD~1
- Removes the commit
- Discards the changes

-------------------------------------------------------------------------------
Undo Uncommitted Changes

git restore file
git restore .

===============================================================================
Branch Operations
===============================================================================

Create Branch
  git switch -c feature

Switch Branch
  git switch main

Delete Branch
  git branch -d branch

Force Delete
  git branch -D branch

Delete Remote Branch
  git push origin --delete branch

===============================================================================
Clone vs Fork
===============================================================================

Clone Creates a Local copy on User's machine

Fork Creates a Personal remote copy on the cloud repo

===============================================================================
Fetch vs Pull
===============================================================================

git fetch
- Downloads remote changes
- Doesn't modify current local branch but updates the remote-tracking branch

            Remote
            A --- B --- C

Local
A --- B

origin/main
A --- B 

After fetch

Local
A --- B

origin/main
A --- B --- C

--------------------------------------------------

git pull

git fetch + git merge

Remote
A --- B --- C

Local

A --- B --- C

===============================================================================
Merge vs Rebase
===============================================================================

MERGE
- Combines histories
- Creates Merge Commit
- Doesn't rewrite history

A---B---C
    \
     D---E

Merge

A---B---C------M
    \         /
     D---E---/

--------------------------------------------------

REBASE

- Replays commits
- Linear history
- Rewrites history

Before

A---B---C
    \
     D---E

After

A---B---C
          \
           D'---E'

Never rebase shared branches.

===============================================================================
Merge Types
===============================================================================

Fast Forward

A fast-forward merge happens when the target branch has no new commits since the feature branch was created.
Instead of creating a merge commit, Git simply moves the branch pointer forward.

A---B---C
         \
          D---E

Result

A---B---C---D---E

Command: 
  git merge feature

--------------------------------------------------

Squash Merge

A squash merge combines all commits from a feature branch into a single commit before merging it into the target branch.

A---B---C
    \
     D---E---F

Result

A---B---C---G

G = Single Squashed Commit

Command:
  git merge --squash feature

-------------------------------------------------------------------------------
Merge Conflict

Occurs when the same lines are modified differently.

Resolve:
- Edit file
- git add
- Complete merge/rebase

===============================================================================
Useful Commands
===============================================================================

git log
git log --oneline
git log --graph --all

--------------------------------------------------

git diff                    -->      Compares Working Directory  <->  Staging Area (Index)
git diff --staged           -->      Compares Staging Area  <->  Last Commit (HEAD)
git diff HEAD               -->      Compares Working Directory + Staging Area  <->  HEAD
git diff commit1 commit2    -->      Compares any two commits
git diff main feature       -->      Compares any two branches

-------------------------------------------------------------------------------
git stash

Temporarily saves uncommitted changes.

git stash
git stash pop

-------------------------------------------------------------------------------
git cherry-pick

Copies selected commit(s) to another branch.

main
A---B---C

feature
A---B---C---D---E

git cherry-pick D

main
A---B---C---D'

===============================================================================
Remote Collaboration
===============================================================================

CI/CD Flow

                            Developer
                              |
                              v
                        Pull/Merge Request
                              |
                              v
                              CI
                              |
                          Tests Pass
                              |
                              v
                            Merge
                              |
                              v
                            Deploy

-------------------------------------------------------------------------------
Branch Protection

- Prevents direct pushes to the main/master
- Require PR/MR review
- Require CI checks like pipeline pass
- Prevents branch deletion

===============================================================================
Git Tags
===============================================================================

Tag = Named reference to a commit -> Helps to remember the commit pushed during releases specifically
      Unlike a branch, a tag never moves.


git tag v1.0.0 D

A --- B --- C --- D(v1.0.0) --- E -- F -- G

main moves to G
Tag stays on D


MAJOR USECASE:

Imagine production crashes.

Current history:

  A --- B --- C(v1.0.0) --- D --- E --- F(v2.0.0)

You can immediately go back to the exact code released as v1.0.0:

  git checkout v1.0.0

No need to remember the commit hash.


Create a tag on current commit
  git tag v1.0.0

Create a tag on a specific commit
  git tag v1.0.0 <commit-hash>

List tags
  git tag

Checkout a tag
  git checkout v1.0.0

Push a tag
  git push origin v1.0.0

Push all tags
  git push --tags

===============================================================================
Branching Strategy (Typical Company)
===============================================================================

                  feature_branch
                        |
                        v
                  pushed to dev
                        |
                        v
                pushed to feature-int
                        |
                        v
                  pushed to prod-int
                        |
                        v
                      prod
